To install the package just run
> ./install

It will generate the irstlm library and commands, respectively, under
the directories ./lib and ./bin.

Documentation about the use of the irstlm package is available under 
the directory ./doc.



